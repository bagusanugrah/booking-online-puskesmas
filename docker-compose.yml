services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    # Kita suruh Docker membaca file .env yang ada di folder backend
    env_file:
      - ./backend/.env 

  frontend:
    build: ./frontend
    ports:
      - "80:80"